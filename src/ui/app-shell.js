// src/ui/app-shell.js — sidebar + mobile drawer shell.
// Phase 2 stub: the shell DOM arrives in Phase 3; the router already calls
// setActive/closeDrawer, which must be safe no-ops until then.

export function setActive(/* { sid, page } */) {}
export function closeDrawer() {}
